# 要件定義書 — 画像変換ツール (gazouhenkan)

**作成日**: 2026-06-04  
**バージョン**: 1.0  
**リポジトリ**: https://github.com/mabuy2-ops/gazouhenkan

---

## 1. プロジェクト概要

### 1.1 目的
WebブラウザからアクセスできるシンプルなUI で、以下の3種類の画像変換処理をワンクリックで実行・ダウンロードできるWebアプリを提供する。

### 1.2 スコープ
| 機能 | 概要 |
|------|------|
| WebP → JPEG 変換 | WebP 画像を高品質 JPEG に変換してダウンロード |
| 背景透明化 | AI を用いて画像の背景を自動除去し、透過 PNG を生成 |
| ベクター化 | ラスター画像 (JPG/PNG) をスケーラブルな SVG に変換 |

---

## 2. 機能要件

### 2.1 WebP → JPEG 変換

| 項目 | 内容 |
|------|------|
| 入力 | WebP 形式の画像ファイル |
| 出力 | JPEG ファイル（品質 95%）|
| ファイル名 | `{元ファイル名}_{YYYYmmdd_HHMMSS}.jpg` |
| 処理 | PIL で RGB 変換後、JPEG エンコード |

### 2.2 背景透明化

| 項目 | 内容 |
|------|------|
| 入力 | JPG / PNG / WebP など一般的な画像形式 |
| 出力 | 透過 PNG ファイル |
| ファイル名 | `{元ファイル名}_nobg_{YYYYmmdd_HHMMSS}.png` |
| 処理 | rembg ライブラリ（機械学習モデル）による自動背景除去 |
| 注意 | 初回実行時はモデルのダウンロードが発生し、処理に時間がかかる |

### 2.3 ベクター化 (SVG 変換)

| 項目 | 内容 |
|------|------|
| 入力 | JPG / PNG 形式の画像ファイル |
| 出力 | SVG ファイル |
| ファイル名 | `{元ファイル名}_{色数}colors_{YYYYmmdd_HHMMSS}.svg`（カラーモード） |
| 処理 | vtracer ライブラリによるラスター→ベクター変換 |
| 最大解像度 | 512px（長辺）に自動リサイズ（OOM 防止） |

#### ベクター化パラメータ

| パラメータ | 型 | 範囲 | デフォルト | 説明 |
|-----------|-----|------|-----------|------|
| mode | 選択 | color / bw | color | カラーモード / 白黒モード |
| colors | 整数 | 2 〜 256 | 16 | 使用色数（カラーモード時） |
| filter_speckle | 整数 | 1 〜 100 | 4 | ノイズ除去の強度 |
| corner_threshold | 整数 | 1 〜 180 | 60 | コーナー判定の閾値（度） |
| length_threshold | 実数 | 1.0 〜 10.0 | 4.0 | 最短パス長 |
| splice_threshold | 整数 | 1 〜 180 | 45 | パス結合の角度閾値（度） |
| path_mode | 選択 | spline / polygon / none | spline | パスの描画方式 |

---

## 3. 非機能要件

### 3.1 パフォーマンス
- ベクター化処理のタイムアウト: 120 秒
- 入力画像は長辺 512px 以内にリサイズして処理（メモリ節約）
- Gunicorn ワーカー数: 1（無料ホスティング環境向け）

### 3.2 可用性
- ヘルスチェックエンドポイント `/healthz` を提供
- Render.com の自動ヘルスチェックに対応

### 3.3 セキュリティ
- ファイルはメモリ上で処理し、サーバーには保存しない
- ユーザーが送信したパラメータはサーバー側で範囲検証を実施

### 3.4 互換性
- モダンブラウザ（Chrome / Firefox / Edge / Safari 最新版）
- スマートフォン・タブレット対応（レスポンシブレイアウト）

---

## 4. システム構成

### 4.1 アーキテクチャ

```
ブラウザ (HTML/CSS/JS)
        ↕ HTTP (multipart/form-data)
Flask アプリ (app.py)
        ↕
PIL / rembg / vtracer
```

### 4.2 技術スタック

| レイヤー | 技術 |
|---------|------|
| フロントエンド | HTML5 / CSS3 / Vanilla JavaScript |
| バックエンド | Python 3.12 / Flask |
| 本番サーバー | Gunicorn |
| 画像処理 | Pillow, rembg, vtracer, onnxruntime |
| コンテナ | Docker (python:3.12-slim) |
| ホスティング | Render.com |

### 4.3 APIエンドポイント

| メソッド | パス | 入力 | 出力 | 説明 |
|---------|------|------|------|------|
| GET | `/` | — | HTML | メイン画面 |
| GET | `/healthz` | — | テキスト | ヘルスチェック |
| POST | `/convert/webp` | file | JPEG | WebP→JPEG変換 |
| POST | `/convert/bg` | file | PNG | 背景除去 |
| POST | `/convert/svg` | file + params | SVG | ベクター化 |

---

## 5. ファイル構成

```
gazouhenkan/
├── app.py              # Flask バックエンド（全APIロジック）
├── templates/
│   └── index.html      # フロントエンド UI（Single Page）
├── requirements.txt    # Python 依存ライブラリ
├── Dockerfile          # Docker ビルド定義
├── render.yaml         # Render.com デプロイ設定
└── .python-version     # Python バージョン指定 (3.12.0)
```

---

## 6. 制約・前提条件

- Python 3.12 必須（vtracer が 3.11 以前で SIGSEGV を起こすことがある）
- rembg の初回実行時は機械学習モデルのダウンロードが必要（数百 MB）
- 無料ホスティング（Render.com）のメモリ制限に対応するため、画像サイズを 512px に制限
- vtracer は内部的に Rust 実装であり、プラットフォーム依存のバイナリ

---

## 7. 将来的な拡張候補（スコープ外）

- バッチ処理（複数ファイル一括変換）
- 変換履歴の保存
- SVG 後処理（Inkscape 連携など）
- より高解像度への対応（メモリ増強が前提）
